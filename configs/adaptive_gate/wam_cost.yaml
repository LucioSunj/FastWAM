# Analytical placeholder for the binary adaptive gate.
# Replace this file with measured FLOPs from scripts/profile_wam_modes.py before
# training; both entries include the one shared VAE encode per gate decision.
version: 3
source: analytical
mode_order: [uncond, idm]
modes:
  uncond: 0.15
  idm: 1.0
meta:
  note: placeholder only; profile the exact checkpoint, resolution and horizon
  inference_steps: 20
